# Choose the local LAN interface Wi-Fi
interface=wlan0
# bind
bind-interfaces
# tell clients default gateway is the pi-router
dhcp-option=3,10.0.50.1
# tell clients to use the pi's DNS server
dhcp-option=6,10.0.50.1
dhcp-range=10.0.50.50,10.0.50.250,12h
